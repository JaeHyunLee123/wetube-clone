extends ../base
include ../mixins/video

block content
  form(method="GET")
    input(name="keyword" placeholder="Seach by title" type="text")
    input(type="submit" value="Search now")

  each video in videos 
    +video(video)